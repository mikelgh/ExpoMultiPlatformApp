# Git 远程仓库配置说明 | Git Remote Repository Configuration

## 📦 配置的远程仓库 | Configured Remote Repositories

本项目已配置为同时推送到两个远程仓库：

### 1️⃣ GitHub (主仓库)
- **URL**: https://github.com/mikelgh/ExpoMultiPlatformApp.git
- **用途**: 国际用户访问，GitHub Actions CI/CD

### 2️⃣ Gitee (国内镜像)
- **URL**: https://gitee.com/mikelgh/expo-multi-platform-app.git
- **用途**: 国内用户快速访问，加速克隆和下载

---

## 🔧 当前配置 | Current Configuration

```bash
# 查看远程仓库配置
$ git remote -v

origin  https://github.com/mikelgh/ExpoMultiPlatformApp.git (fetch)
origin  https://gitee.com/mikelgh/expo-multi-platform-app.git (push)
origin  https://github.com/mikelgh/ExpoMultiPlatformApp.git (push)
```

**说明**:
- `fetch`: 只从 GitHub 拉取
- `push`: 同时推送到 Gitee 和 GitHub

---

## 🚀 使用方法 | Usage

### 推送代码 | Push Code

一次推送，同时更新两个仓库：

```bash
git push origin master
```

或推送所有分支：

```bash
git push origin --all
```

**自动同步到**:
- ✅ GitHub: https://github.com/mikelgh/ExpoMultiPlatformApp
- ✅ Gitee: https://gitee.com/mikelgh/expo-multi-platform-app

### 拉取代码 | Pull Code

只从 GitHub 拉取：

```bash
git pull origin master
```

### 推送标签 | Push Tags

```bash
# 推送单个标签
git push origin v1.0.0

# 推送所有标签到两个仓库
git push origin --tags
```

---

## 📝 配置步骤记录 | Configuration Steps

如需重新配置或在其他项目中使用相同配置：

```bash
# 1. 查看当前远程仓库
git remote -v

# 2. 添加 Gitee 为第一个推送地址
git remote set-url --add --push origin https://gitee.com/mikelgh/expo-multi-platform-app.git

# 3. 添加 GitHub 为第二个推送地址
git remote set-url --add --push origin https://github.com/mikelgh/ExpoMultiPlatformApp.git

# 4. 验证配置
git remote -v

# 5. 测试推送
git push origin master
```

---

## 🔄 高级操作 | Advanced Operations

### 只推送到 GitHub

```bash
git push https://github.com/mikelgh/ExpoMultiPlatformApp.git master
```

### 只推送到 Gitee

```bash
git push https://gitee.com/mikelgh/expo-multi-platform-app.git master
```

### 添加额外的远程仓库

```bash
# 添加单独的 Gitee remote（可选）
git remote add gitee https://gitee.com/mikelgh/expo-multi-platform-app.git

# 单独推送到 Gitee
git push gitee master
```

### 修改配置

```bash
# 移除所有 push URL
git remote set-url --delete --push origin <URL>

# 重新添加
git remote set-url --add --push origin <NEW_URL>
```

---

## ⚠️ 注意事项 | Important Notes

### 1. 首次推送到 Gitee

如果 Gitee 仓库是空的或刚创建：

```bash
# 强制推送（仅首次）
git push -f origin master

# 或推送所有分支和标签
git push -u origin --all
git push origin --tags
```

### 2. 认证问题

如果遇到认证错误：

**方案 1: 使用 SSH**
```bash
# 修改为 SSH URL
git remote set-url --delete --push origin https://gitee.com/mikelgh/expo-multi-platform-app.git
git remote set-url --add --push origin git@gitee.com:mikelgh/expo-multi-platform-app.git
```

**方案 2: 使用个人访问令牌**
- GitHub: Settings → Developer settings → Personal access tokens
- Gitee: 设置 → 安全设置 → 私人令牌

### 3. 推送失败处理

如果某个仓库推送失败：

```bash
# 查看详细错误
git push origin master --verbose

# 单独推送到失败的仓库
git push https://gitee.com/mikelgh/expo-multi-platform-app.git master
```

---

## 📊 仓库信息对比 | Repository Comparison

| 特性 | GitHub | Gitee |
|------|--------|-------|
| 国际访问速度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 国内访问速度 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 开源生态 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| CI/CD | GitHub Actions | Gitee Go |
| Issue 跟踪 | ✅ | ✅ |
| Wiki | ✅ | ✅ |
| Pages | ✅ | ✅ |

---

## 🎯 推荐工作流 | Recommended Workflow

### 日常开发 | Daily Development

```bash
# 1. 创建新分支
git checkout -b feature/new-feature

# 2. 开发和提交
git add .
git commit -m "feat: add new feature"

# 3. 推送到两个仓库
git push origin feature/new-feature

# 4. 合并到主分支
git checkout master
git merge feature/new-feature
git push origin master
```

### 版本发布 | Release

```bash
# 1. 创建版本标签
git tag -a v1.0.0 -m "Release version 1.0.0"

# 2. 推送标签到两个仓库
git push origin --tags

# 3. 在 GitHub 和 Gitee 上创建 Release
```

---

## 📚 相关资源 | Related Resources

### GitHub 仓库
- **URL**: https://github.com/mikelgh/ExpoMultiPlatformApp
- **Clone**: `git clone https://github.com/mikelgh/ExpoMultiPlatformApp.git`

### Gitee 仓库
- **URL**: https://gitee.com/mikelgh/expo-multi-platform-app
- **Clone**: `git clone https://gitee.com/mikelgh/expo-multi-platform-app.git`

### 快速克隆建议

**国内用户**:
```bash
git clone https://gitee.com/mikelgh/expo-multi-platform-app.git
```

**国际用户**:
```bash
git clone https://github.com/mikelgh/ExpoMultiPlatformApp.git
```

---

## ✅ 配置验证 | Configuration Verification

运行以下命令验证配置正确：

```bash
# 1. 查看远程仓库
git remote -v
# 应该看到 1 个 fetch 和 2 个 push

# 2. 测试推送（dry-run）
git push origin master --dry-run

# 3. 实际推送
git push origin master
# 应该看到同时推送到两个仓库的输出
```

---

**配置日期**: 2025-11-25  
**配置状态**: ✅ 已完成并测试  
**维护者**: mikelgh
